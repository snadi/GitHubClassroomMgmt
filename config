[DEFAULT]
# Github username
username = 1asso 

# File containing your github token
token = token_file.txt

# Course github organization
organization = cmput201-w21

# Assignment or lab name, only collect repos with this prefix (e.g., labs-1asso)
prefix = labs

# CCID list
# Provide no value to collect all repos
ccid_list = ccids.txt

# Repo name list
# If a value is provided, the script won't look at the ccid_list
repo_name_list = 

# Deadline of the given assignment or lab in format \"YYYY-MM-DD HH:MM:SS -0700\" where -0700 indicates the MST time zone
# Provide no value to collect repos with the latest commit
deadline = 2021-01-13 00:00:00 -0700

# Assignment pair list
pairs = pairs.csv

# GitHub Classroom roster
roster = roster.csv